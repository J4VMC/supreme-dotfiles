#!/bin/sh
# Global post-merge hook: defers to the repository's lefthook config, if any.
# See lefthook-dispatch in this directory for why this exists.
exec "$(dirname "$0")/lefthook-dispatch" post-merge "$@"
