#!/bin/sh
# Global post-checkout hook: defers to the repository's lefthook config, if any.
# See lefthook-dispatch in this directory for why this exists.
exec "$(dirname "$0")/lefthook-dispatch" post-checkout "$@"
